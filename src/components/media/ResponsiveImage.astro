---
type Props = {
  src: string;
  alt: string;
  loading?: "eager" | "lazy";
  sizes?: string;
};

const {
  src,
  alt,
  loading = "lazy",
  sizes = "(min-width: 900px) 50vw, 100vw",
} = Astro.props;
---

<img src={src} alt={alt} loading={loading} decoding="async" sizes={sizes} />
